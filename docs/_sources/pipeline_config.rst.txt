***************************
pipeline configuration file
***************************

The YAML configuration file used by the pipeline. All paths are
relative to the location of the `hst_cosmic_rays` installation.

.. literalinclude:: ../../CONFIG/pipeline_config.yaml
  :language: YAML